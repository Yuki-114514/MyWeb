---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { projects } from "@/data/projects";
import ProjectCard from "@/components/ProjectCard.astro";
import Icon from "@/components/Icon.astro";

const sorted = [...projects].sort((a, b) => b.year.localeCompare(a.year));
---

<BaseLayout title="项目" description="我做过和正在做的东西。">
  <section class="container">
    <header class="page-head rise">
      <span class="eyebrow">✦ Projects</span>
      <h1 class="page-title">我做过的东西</h1>
      <p class="page-lede">
        有战队里的团队项目，也有自己一个人从头写的东西。
      </p>
    </header>

    <div class="proj-grid">
      {
        projects.length === 0 ? (
          <p class="empty">还没有项目，敬请期待呀 <Icon name="plant" /></p>
        ) : (
          sorted.map((p, i) => (
            <div class="reveal" data-delay={((i % 3) + 1) as 1 | 2 | 3}>
              <ProjectCard project={p} headingLevel={2} />
            </div>
          ))
        )
      }
    </div>
  </section>
</BaseLayout>

<style>
  .page-head {
    max-width: 40rem;
    margin-bottom: 3rem;
    padding-top: 2rem;
  }
  .page-title {
    font-size: var(--text-2xl);
    margin-top: 0.6rem;
    margin-bottom: 0.9rem;
  }
  .page-lede {
    color: var(--ink-soft);
    font-size: var(--text-lg);
  }
  .proj-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(16rem, 1fr));
    gap: 1.4rem;
  }
  .empty {
    color: var(--ink-faint);
  }
</style>
